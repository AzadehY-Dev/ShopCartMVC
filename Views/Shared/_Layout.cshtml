<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
    <meta name="description" content="">
    <meta name="author" content="">

    <title>TopLearn.Com</title>

    <!-- Bootstrap core CSS -->
    <link href="/Content/bootstrap.min.css" rel="stylesheet">
    <link href="/Content/bootstrap-rtl.css" rel="stylesheet" />

    <!-- Custom styles for this template -->
    <link href="/Content/justified-nav.css" rel="stylesheet">


</head>

<body>

<div class="container">

    <!-- The justified navigation menu is meant for single line per list item.
    Multiple lines will require custom code not provided by Bootstrap. -->
    <div class="masthead">
        <h3 class="text-muted text-left">TopLearn.Com</h3>
        <nav>
            <ul class="nav nav-justified">
                <li class="active"><a href="/">Home</a></li>
                <li><a href="/ShopCart">
                    <i class="glyphicon glyphicon-shopping-cart"> سبد خرید : <span id="shopcart">0</span> کالا</i>
                </a></li>
 
            </ul>
        </nav>
    </div>

    <!-- Jumbotron -->
    <div class="jumbotron">
        <a href="https://TopLearn.Com">
        <img src="https://barnamenevisan.org/Images/Ads/5eb4a4fa0e9042a5b95989ca270d0ded.gif"/>
        </a>
    </div>

    <!-- Example row of columns -->
    <div class="row">
        @RenderBody()
    </div>

    <!-- Site footer -->
    <footer class="footer">
        <p>&copy; 2016 Company, Inc.</p>
    </footer>

</div> <!-- /container -->
<script src="~/Scripts/jquery-1.10.2.min.js"></script>
<script>
    function AddToCart(id) {
        $.ajax({
            url: "/ShopCart/AddToCart/" + id,
            type: "Get"

        }).done(function (result) {
            $('#shopcart').html(result);
        });
    }
    //function AddToCart(id) {
    //    $.ajax({
    //        url: "/ShopCart/AddToCart/" + id,
    //        type: "Get"
    //    }).done(function(result) {
    //        $("#shopcart").html(result);
    //    });
    //}

    $(function() {
        $("#shopcart").load("/ShopCart/ShopCartCount");
    });
</script>

</body>
</html>
